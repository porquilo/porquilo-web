# In development, leave this unset — Vite proxies /api to localhost:4184 automatically.
# Only set this to override in production or when running without the Vite dev server.
# VITE_API_BASE_URL=https://your-production-api.example.com
